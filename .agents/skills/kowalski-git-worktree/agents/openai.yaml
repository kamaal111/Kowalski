interface:
  display_name: 'Kowalski Git Worktree'
  short_description: 'Isolated worktree branch, env, and PR workflow'
  default_prompt: 'Use $kowalski-git-worktree to prepare an isolated Kowalski worktree, attach or publish the branch, and open or update the GitHub PR.'
